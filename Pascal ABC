Program Fibonacci;
var
  i, n, s, a, a1: integer;
 
begin
  readln(n);  
  for i := 1 to n do
  begin
    if (i = 1) or (i = 2) then
      s := 1
    else
      s := a + a1;
    a := a1;
    a1 := s;    
    writeln(s);
  end;  
end. 